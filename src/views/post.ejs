<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= post.title %> - ThauanyHeloisa Blog</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

  <style>
    article {
      font-family: 'Arial', sans-serif;
      line-height: 1.6;
      color: #333;
    }

    /* Cabeçalhos */
    article h1, article h2, article h3, article h4, article h5, article h6 {
      font-weight: bold;
      margin-bottom: 1rem;
      color: #4a4a4a;
    }
    article h1 {
      font-size: 2.5rem;
    }
    article h2 {
      font-size: 2rem;
    }
    article h3 {
      font-size: 1.75rem;
    }
    article h4 {
      font-size: 1.5rem;
    }
    article h5 {
      font-size: 1.25rem;
    }
    article h6 {
      font-size: 1rem;
    }

    /* Parágrafos */
    article p {
      margin-bottom: 1.25rem;
      font-size: 1.125rem;
      color: #4a4a4a;
    }

    /* Links */
    article a {
      color: #5a67d8;
      text-decoration: underline;
    }
    article a:hover {
      color: #434190;
    }

    /* Listas ordenadas e não ordenadas */
    article ul {
      list-style-type: disc;
      margin-left: 1.5rem;
      margin-bottom: 1.25rem;
    }
    article ol {
      list-style-type: decimal;
      margin-left: 1.5rem;
      margin-bottom: 1.25rem;
    }
    article li {
      margin-bottom: 0.75rem;
    }

    /* Listas de checagem (checkbox) */
    article input[type="checkbox"] {
      margin-right: 0.5rem;
    }

    /* Negrito, itálico e sublinhado */
    article b, article strong {
      font-weight: bold;
    }
    article i, article em {
      font-style: italic;
    }
    article u {
      text-decoration: underline;
    }

    /* Alinhamento */
    article .ql-align-right {
      text-align: right;
    }
    article .ql-align-center {
      text-align: center;
    }
    article .ql-align-justify {
      text-align: justify;
    }

    /* Imagens */
    article img {
      max-width: 100%;
      height: auto;
      margin-bottom: 1.25rem;
    }

    /* Vídeos */
    article iframe, article video {
      max-width: 100%;
      height: auto;
      margin-bottom: 1.25rem;
    }

    /* Cores do texto */
    article [style*="color"] {
      color: inherit;
    }

    /* Fundo */
    article [style*="background-color"] {
      background-color: inherit;
    }

    /* Bloco de citação */
    article blockquote {
      padding: 0.75rem 1.25rem;
      background-color: #f7fafc;
      border-left: 4px solid #cbd5e0;
      margin-bottom: 1.25rem;
      font-style: italic;
      color: #2d3748;
    }

    /* Limpar formatação */
    article .ql-clean {
      all: unset;
    }

  </style>


<body class="bg-gray-50 min-h-screen flex flex-col">

  <!-- Header -->
  <header class="bg-purple-600 text-white">
    <div class="max-w-7xl mx-auto p-4 flex justify-between items-center">
      <h1 class="text-3xl font-bold">ThauanyHeloisa Blog</h1>
      <nav class="hidden md:flex">
        <a href="#" class="text-white hover:text-gray-200 mx-2">Home</a>
        <a href="#" class="text-white hover:text-gray-200 mx-2">Sobre</a>
        <a href="#" class="text-white hover:text-gray-200 mx-2">Contato</a>
      </nav>
      <button id="menu-toggle" class="md:hidden p-2 text-white focus:outline-none">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7" />
        </svg>
      </button>
    </div>
  </header>

  <!-- Main Content -->
  <main class="max-w-7xl mx-auto py-8 px-4 flex-1">
    <!-- Título e Categoria -->
    <div class="mb-4">
      <h2 class="text-3xl font-bold mb-2"><%= post.title %></h2>
      <p class="text-gray-600">Categoria: <%= post.category %></p>
      <p class="text-gray-600 text-sm">Publicado em: <%= post.createdAt.toLocaleDateString('pt-BR') %> às <%= post.createdAt.toLocaleTimeString('pt-BR') %></p>
    </div>

    <!-- Conteúdo da Publicação -->
    <article class="prose prose-lg max-w-none bg-white p-6 rounded shadow">
      <%- post.content %>
    </article>

    <!-- Botão para voltar -->
    <div class="mt-6">
      <a href="/" class="text-purple-600 hover:text-purple-800">← Voltar para a lista de publicações</a>
    </div>
  </main>

  <!-- Overlay -->
  <div id="menu-overlay" class="menu-overlay fixed inset-0 bg-black bg-opacity-50 hidden"></div>

  <!-- Sidebar Menu -->
  <aside id="sidebar" class="fixed inset-0 bg-gray-800 bg-opacity-75 z-50 hidden md:hidden">
    <div class="flex flex-col bg-white w-64 h-full">
      <button id="close-menu" class="self-end p-4">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 text-gray-800" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
        </svg>
      </button>
      <nav class="flex flex-col p-4">
        <a href="#" class="py-2 hover:bg-gray-200">Home</a>
        <a href="#" class="py-2 hover:bg-gray-200">Sobre</a>
        <a href="#" class="py-2 hover:bg-gray-200">Contato</a>
      </nav>
    </div>
  </aside>

  <!-- Footer -->
  <footer class="bg-gray-800 text-white mt-10">
    <div class="max-w-7xl mx-auto p-6 flex flex-col md:flex-row justify-between items-center">
      <div class="mb-4 md:mb-0">
        <h5 class="text-lg font-bold">ThauanyHeloisa</h5>
        <p>&copy; 2024 Todos os direitos reservados.</p>
        <p>Desenvolvido com carinho.</p>
      </div>
      <div class="flex flex-col md:flex-row md:space-x-6">
        <a href="#" class="hover:text-gray-300">Sobre nós</a>
        <a href="#" class="hover:text-gray-300">Contato</a>
        <a href="#" class="hover:text-gray-300">Política de Privacidade</a>
        <a href="#" class="hover:text-gray-300">Termos de Serviço</a>
      </div>
    </div>
  </footer>

  <script>
    const menuToggle = document.getElementById('menu-toggle');
    const sidebar = document.getElementById('sidebar');
    const menuOverlay = document.getElementById('menu-overlay');
    const closeMenu = document.getElementById('close-menu');

    menuToggle.addEventListener('click', () => {
      sidebar.classList.toggle('hidden');
      menuOverlay.classList.toggle('hidden');
    });

    closeMenu.addEventListener('click', () => {
      sidebar.classList.add('hidden');
      menuOverlay.classList.add('hidden');
    });

    menuOverlay.addEventListener('click', () => {
      sidebar.classList.add('hidden');
      menuOverlay.classList.add('hidden');
    });
  </script>
</body>
</html>
